/**
 Abstraction is essentially "need to know". We hide the major implementation details, while keeping the obvious usage at the forefront. 
 So remember this "hide the details, show the relevance".
*/
